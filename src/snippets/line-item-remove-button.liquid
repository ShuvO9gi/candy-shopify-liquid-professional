<button class="flex items-center mt-5 py-1 px-3 border-2 border-orange border-solid rounded-xl text-orange" data-{{ data }} data-id="{{ id }}">
  <span class="mr-4 font-semibold uppercase">{{ 'cart.general.remove' | t }}</span>
  <span>{%- render 'icon' with 'trash' -%}</span>
</button>